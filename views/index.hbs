<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}}</title>
</head>
<body>
  <h1>{{title}}</h1>

  {{#if error}}
    <h2>Error: {{error}}</h2>
  {{/if}}
  <ul>
    {{#each features}}
      <li>{{this.name}}: {{this.value}}</li>
    {{/each}}    
  </ul>

</body>
</html>